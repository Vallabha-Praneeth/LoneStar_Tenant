import * as React from 'react';
import * as ReactDOM from 'react-dom/client';
import { OrgWebApp } from './app';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <OrgWebApp />
  </React.StrictMode>,
);
