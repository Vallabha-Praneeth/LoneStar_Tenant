{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": "."
  },
  "include": [
    "app",
    "assets",
    "components",
    "constants",
    "context",
    "hooks",
    "../shared"
  ]
}
